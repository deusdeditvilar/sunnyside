import React from "react";

export interface globalProps {
    className?: string,
    children?: React.ReactNode,
    id?: string,
    href?: string,
    reverse?: boolean,
    column?: boolean,
    style?: {}
}